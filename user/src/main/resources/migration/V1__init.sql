
CREATE TABLE emprunteur(
                     id SERIAL NOT NULL PRIMARY KEY,
                     firstname VARCHAR (100),
                     lastname VARCHAR (20),
                     email VARCHAR (20)
);